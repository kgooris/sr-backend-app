{% extends 'AdminDefault/index.html.twig' %}

{% block body %}
<div class="container">
<div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
    <h1 class="sub-header">DrankBestellingen</h1>
<div class="table-responsive">
    <table class="table table-condensed table-bordered table-hover">
        <thead>
            <tr>
                <th class="col-md-4">Drankstand</th>
                <th class="col-md-2">AanvraagDatum</th>
                <th class="col-md-2">Min. Open</th>
                <th class="col-md-1">Wie</th>
                <th class="col-md-3">Acties</th>
                
            </tr>
        </thead>
        <tbody>
        {% for order in orders %}
            <tr>
                <td>{{ order.drankstand.naam }}</td>
                <td>{% if order.createdAt %}{{ order.createdAt|date('Y-m-d H:i') }}{% endif %}</td>
                {% set difference = date("now").diff(date(order.createdAt)) %}
                {% set minutespassed = difference.i %}
                {% set hourspassed = difference.h %}
                {% set dayspassed = difference.days %}
                {% set minutes = (dayspassed *24 *60)+(hourspassed*60)+minutespassed %}
                <td align="center">{{ minutes }}</td>
                <td>{{ order.owner }}</td>
                <td align="center">
                        <a class="btn btn-default" href="{{ path('admin_drankbestelling_show', { 'id': order.id }) }}">
                        	<span class="glyphicon glyphicon-eye-open" aria-hidden="true"></span> Details
                        </a>
                        <a class="btn btn-default" target="_blank" href="{{ path('admin_drankbestelling_print', { 'id': order.id , 'finalize': '0'}) }}">
                        	<span class="glyphicon glyphicon-edit" aria-hidden="true"></span> Printen
                        </a>
                        <a class="btn btn-default" href="{{ path('admin_drankbestelling_levershow', { 'id': order.id }) }}">
                            <span class="glyphicon glyphicon-edit" aria-hidden="true"></span> maak levering
                        </a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
    
           <a class="btn btn-default btn-lg" href="{{ path('admin_drankbestelling_new') }}">
                            	<span class="glyphicon glyphicon-plus" aria-hidden="true"></span> Toevoegen
                     	</a>
           
        
    
    </div>
    </div>
{% endblock %}
