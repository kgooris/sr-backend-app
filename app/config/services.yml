# Learn more about services, parameters and containers at
# http://symfony.com/doc/current/book/service_container.html
parameters:
#    parameter_name: value
#      app.file_type_extension:
 #         class: AppBundle\Form\Extension\FileTypeExtension

services:
#    service_name:
#        class: AppBundle\Directory\ClassName
#        arguments: ["@another_service_name", "plain_value", "%parameter_name%"]
 #    app.file_type_extension:
  #       class: AppBundle\Form\Extension\FileTypeExtension
   #      tags:
    #        - { name: form.type_extension, alias: file }
      security.role_hierarchy:
          class: AppBundle\Service\RoleHierarchyHelper
          arguments: ["%security.role_hierarchy.roles%", "@doctrine.orm.default_entity_manager"]
      app.smsmessage:
          class: AppBundle\Utils\SMSMessage
          arguments: ['@logger', '@doctrine.orm.entity_manager']

